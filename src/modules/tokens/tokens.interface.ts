export interface Token {
  token_id: string;
  token: string;
  business_id: string;
  type: string;
  service: string;
}

export interface TokenData {
  tokenId: string;
  token: string;
  businessId: string;
  type: string;
  service: string;
}
