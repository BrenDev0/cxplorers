export interface Token {
  token_id?: string;
  token: string;
  user_id: string;
  type: string;
  service: string;
}

export interface TokenData {
  tokenId?: string;
  token: string;
  userId: string;
  type: string;
  service: string;
}
